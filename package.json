{
  "name": "stuporbowl-v3",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "concurrently -k -n astro,express -c cyan,yellow \"astro dev --host\" \"nodemon --watch server.js --watch server/ server.js\"",
    "build": "astro build",
    "heroku-postbuild": "npm run build",
    "preview": "astro preview",
    "start": "node server.js",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.23.2",
    "cors": "^2.8.6"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "nodemon": "^3.1.14"
  }
}
